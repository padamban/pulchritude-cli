// import { asArray } from '../as-array'

describe('asArray', () => {
  test('demo', () => {
    expect(!!true).toBe(true)
  })

  // test('wraps number into an array', () => {
  //   expect(asArray(88)).toMatchObject([88])
  // })

  // test('wraps null into an empty array', () => {
  //   expect(asArray(null)).toMatchObject([])
  // })

  // test('wraps undefined into an empty array', () => {
  //   expect(asArray(undefined)).toMatchObject([])
  // })

  // test('wraps 0 into an array', () => {
  //   expect(asArray(0)).toMatchObject([0])
  // })

  // test('wraps -1 into an array', () => {
  //   expect(asArray(-1)).toMatchObject([-1])
  // })

  // test('wraps object into an array', () => {
  //   expect(asArray({ test: 'ok' })).toMatchObject([{ test: 'ok' }])
  // })

  // test('leaves [number] array as it is', () => {
  //   expect(asArray([66])).toMatchObject([66])
  // })

  // test('leaves [number, number] array as it is (even the reference)', () => {
  //   const data = [45, 46]
  //   expect(asArray(data)).toBe(data)
  // })

  // test('leaves [null] array as it is', () => {
  //   const data = [null]
  //   expect(data).toBe(data)
  // })
})
